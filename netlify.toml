[build]
  command = "npm run build"
  publish = ".next"

# Use Next.js Runtime for proper server-side rendering
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Environment variables (set these in Netlify UI)
# NEXTAUTH_SECRET
# NEXTAUTH_URL
# DEEPSEEK_API_KEY
