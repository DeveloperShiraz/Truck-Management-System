# ============================================
# Truck Management System - Environment Variables
# ============================================

# --------------------------------------------
# NextAuth Configuration (Required)
# --------------------------------------------
# Secret key for JWT token signing and encryption
# IMPORTANT: Generate a secure random string for production
# 
# To generate a secure secret, run one of these commands:
#   - On Linux/Mac: openssl rand -base64 32
#   - On Windows (PowerShell): [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
#   - Online: https://generate-secret.vercel.app/32
#
# Example: abc123XYZ789randomSecretKey456DEF==
NEXTAUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32

# Application URL
# For local development: http://localhost:3000
# For production: https://your-app-name.netlify.app
NEXTAUTH_URL=http://localhost:3000

# --------------------------------------------
# DeepSeek API Configuration (Required)
# --------------------------------------------
# API key for DeepSeek chatbot integration
# Get your API key from: https://platform.deepseek.com/api_keys
#
# Example: sk-1234567890abcdef1234567890abcdef
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# --------------------------------------------
# Node Environment
# --------------------------------------------
# Set to 'production' for production deployment
# Set to 'development' for local development
NODE_ENV=development

# ============================================
# Deployment Instructions
# ============================================
#
# For Netlify Deployment:
# 1. Go to your Netlify site dashboard
# 2. Navigate to Site settings > Environment variables
# 3. Add each variable above with production values:
#    - NEXTAUTH_SECRET: Generate a new secure secret (don't reuse development secret)
#    - NEXTAUTH_URL: Set to your production URL (e.g., https://your-app.netlify.app)
#    - DEEPSEEK_API_KEY: Your DeepSeek API key
#    - NODE_ENV: Set to 'production'
#
# For Local Development:
# 1. Copy this file to .env.local
# 2. Replace placeholder values with actual values
# 3. Never commit .env.local to version control
#
# ============================================
